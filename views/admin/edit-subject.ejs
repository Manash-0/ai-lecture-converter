<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit <%= subject.name %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Edit Subject: <%= subject.name %></h1>
        <form action="/admin/edit-subject/<%= subjectId %>" method="POST">
            <div class="form-group">
                <label for="subjectName">Subject Name:</label>
                <input type="text" id="subjectName" name="subjectName" value="<%= subject.name %>" required>
            </div>
            <div class="form-group">
                <label for="subjectCode">Subject Code:</label>
                <input type="text" id="subjectCode" name="subjectCode" value="<%= subject.code %>" required>
            </div>

            <hr style="margin: 30px 0;">
            <h3>Edit Unit Names</h3>
            <% subject.units.forEach((unit, index) => { %>
                <div class="form-group">
                    <label for="unit<%= index + 1 %>_title">Unit <%= index + 1 %> Title:</label>
                    <input type="text" id="unit<%= index + 1 %>_title" name="unit<%= index + 1 %>_title" value="<%= unit.title %>" required>
                </div>
            <% }) %>

            <button type="submit">Save Changes</button>
            <a href="/admin" style="display: block; text-align: center; margin-top: 15px;">Cancel</a>
        </form>
    </div>

    <button id="theme-toggle">🌙</button>
    <script src="/script.js"></script>
</body>
</html>

=======
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit <%= subject.name %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Edit Subject: <%= subject.name %></h1>
        <form action="/admin/edit-subject/<%= subjectId %>" method="POST">
            <div class="form-group">
                <label for="subjectName">Subject Name:</label>
                <input type="text" id="subjectName" name="subjectName" value="<%= subject.name %>" required>
            </div>
            <div class="form-group">
                <label for="subjectCode">Subject Code:</label>
                <input type="text" id="subjectCode" name="subjectCode" value="<%= subject.code %>" required>
            </div>

            <hr style="margin: 30px 0;">
            <h3>Edit Unit Names</h3>
            <% subject.units.forEach((unit, index) => { %>
                <div class="form-group">
                    <label for="unit<%= index + 1 %>_title">Unit <%= index + 1 %> Title:</label>
                    <input type="text" id="unit<%= index + 1 %>_title" name="unit<%= index + 1 %>_title" value="<%= unit.title %>" required>
                </div>
            <% }) %>

            <button type="submit">Save Changes</button>
            <a href="/admin" style="display: block; text-align: center; margin-top: 15px;">Cancel</a>
        </form>
    </div>

    <button id="theme-toggle">🌙</button>
    <script src="/script.js"></script>
</body>
</html>

>>>>>>> 5727652b69a5727bc4026541f31d5c71089e0237
